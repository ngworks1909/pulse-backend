{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx tsc -b && nodemon ./dist/bin.js",
    "db:generate": "prisma generate --schema=../../packages/db/prisma/schema.prisma",
    "db:push": "prisma db push --schema=../../packages/db/prisma/schema.prisma",
    "build": "npm run db:generate && npx tsc -b",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@types/jest": "^30.0.0",
    "@types/supertest": "^6.0.3",
    "express": "^5.1.0",
    "jest-mock-extended": "^4.0.0",
    "jsonwebtoken": "^9.0.2",
    "supertest": "^7.1.4",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "@types/express": "^5.0.5",
    "@types/jsonwebtoken": "^9.0.10",
    "ts-jest": "^29.4.5"
  }
}
